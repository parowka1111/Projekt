spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/javaigit
    username: postgres
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

security:
  password:
    pepper: "pieprzrzrzrzrz"